'use client';

import { FormEvent, useState } from 'react';
import Link from 'next/link';
import { supabase } from '@/lib/supabase';

export default function PatientLogin() {
  const [email, setEmail] = useState(''); const [notice, setNotice] = useState(''); const [error, setError] = useState(''); const [busy, setBusy] = useState(false);
  const callback = typeof window === 'undefined' ? '' : `${window.location.origin}/auth/callback`;
  async function sendLink(event: FormEvent) { event.preventDefault(); setBusy(true); setError(''); try { const { error } = await supabase.auth.signInWithOtp({ email: email.trim(), options: { emailRedirectTo: callback, shouldCreateUser: true } }); if (error) throw error; setNotice('Check your inbox for a secure sign-in link. New email users are automatically registered.'); } catch (caught) { setError(caught instanceof Error ? caught.message : 'We could not send the sign-in link.'); } finally { setBusy(false); } }
  async function google() { setBusy(true); setError(''); const { error } = await supabase.auth.signInWithOAuth({ provider: 'google', options: { redirectTo: callback } }); if (error) { setError(error.message); setBusy(false); } }
  return <main className="min-h-screen bg-[#f7fbfb] px-5 py-5 text-[#102d36]"><div className="mx-auto max-w-md"><header className="flex items-center justify-between"><Link href="/" className="inline-flex items-center gap-3"><span className="grid h-11 w-11 place-items-center rounded-2xl bg-[#087b88] font-clinical text-lg font-extrabold text-white">AV</span><span><b className="font-clinical text-lg">AarogyaVaani</b><small className="block text-xs text-slate-500">Digital Health System</small></span></Link><Link className="text-sm font-bold text-[#087b88]" href="/doctor/login">Doctor</Link></header><section className="mt-8"><p className="text-xs font-bold tracking-[.18em] text-[#087b88]">YOUR HEALTH · YOUR RECORDS</p><h1 className="mt-3 font-clinical text-3xl font-extrabold leading-tight">Care connected for every patient.</h1><p className="mt-3 text-sm leading-6 text-slate-600">Book appointments, access records, maintain medicine history and share information securely with your care team.</p></section><section className="my-7 rounded-[28px] border border-slate-200 bg-white p-5 shadow-[0_20px_60px_rgba(7,65,72,.12)]"><p className="clinical-eyebrow">PATIENT PORTAL</p><h2 className="mt-2 text-2xl font-extrabold">Sign in to AarogyaVaani</h2><p className="mt-2 text-sm leading-6 text-slate-600">Use Google or email. Your first login creates your patient account.</p><button type="button" onClick={google} disabled={busy} className="mt-6 flex w-full items-center justify-center gap-3 rounded-2xl border border-slate-200 bg-white px-4 py-3.5 font-bold shadow-sm disabled:opacity-50"><span className="grid h-5 w-5 place-items-center rounded-full bg-[#4285f4] text-xs font-black text-white">G</span>Continue with Google</button><div className="my-5 flex items-center gap-3 text-xs text-slate-400"><span className="h-px flex-1 bg-slate-200"/>or<span className="h-px flex-1 bg-slate-200"/></div><form onSubmit={sendLink}><label className="label">Email address</label><input className="input rounded-2xl bg-slate-50" type="email" autoComplete="email" value={email} onChange={event => setEmail(event.target.value)} placeholder="you@example.com" required/><button className="btn-primary mt-4 w-full rounded-2xl py-3.5" disabled={busy}>{busy ? 'Please wait…' : 'Send secure sign-in link'}</button></form>{notice && <p className="mt-4 rounded-2xl bg-cyan-50 p-3 text-sm text-cyan-900">{notice}</p>}{error && <p className="mt-4 rounded-2xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 grid grid-cols-2 gap-3 border-t border-slate-100 pt-5"><Link href="/doctor/login" className="rounded-xl border border-slate-200 px-3 py-3 text-center text-sm font-bold text-slate-700">Doctor portal</Link><Link href="/kiosk" className="rounded-xl border border-slate-200 px-3 py-3 text-center text-sm font-bold text-slate-700">Hospital kiosk</Link></div><p className="mt-5 text-xs leading-5 text-slate-500">AarogyaVaani organizes health information; it does not provide medical diagnosis.</p></section></div></main>;
}
